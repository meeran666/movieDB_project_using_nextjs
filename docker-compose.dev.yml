name: moviemania
services:
  app:
    image: nextjs_image
    container_name: nextjs_movieapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3000"
    depends_on:
      - db
    command: ["bun", "server.js"]
    env_file: ".env.production"
    networks:
      - proxy

networks:
  proxy:
    external: true
